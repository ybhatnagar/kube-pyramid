apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubepyramid.engine.fullname" . }}
  labels:
    {{- include "kubepyramid.labels" . | nindent 4 }}
    app.kubernetes.io/component: engine
spec:
  type: {{ .Values.engine.service.type }}
  selector:
    {{- include "kubepyramid.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: engine
  ports:
    - name: http
      port: {{ .Values.engine.service.port }}
      targetPort: http
